$kind: http-request
url: "{{baseUrl}}/ping"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: "{}"
scripts:
  - type: afterResponse
    code: "pm.test(\"Статус ответа: 405 Method Not Allowed\", function () {\r

      \    pm.expect(pm.response.code).to.equal(405);\r

      });"
    language: text/javascript
order: 2000
