$kind: http-request
url: "{{baseUrl}}/booking?checkin=01-01-2026"
method: GET
queryParams:
  checkin: 01-01-2026
scripts:
  - type: afterResponse
    code: "pm.test(\"Статус ответа: 400 Bad Request\", function () {\r

      \    pm.expect(pm.response.code).to.equal(400);\r

      });\r

      \r

      pm.test(\"Присутствует сообщение об ошибке\", function () {\r

      \    const response = pm.response.text();\r

      \    pm.expect(response).to.include('Invalid date format');\r

      });"
    language: text/javascript
order: 7000
